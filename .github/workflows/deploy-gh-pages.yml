name: Build & Deploy to Staging

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Build Flutter web app
        run: |
          flutter build web \
            --target=lib/main.dart \
            --release \
            --base-href="/" \
            --no-tree-shake-icons \
            --pwa-strategy=none

      - name: Add CNAME and SPA routing
        run: |
          cp web/CNAME build/web/CNAME
          if [ -f staticwebapp.config.json ]; then
            cp staticwebapp.config.json build/web/staticwebapp.config.json
          fi
          cp build/web/index.html build/web/404.html

      - name: Deploy to staging branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          publish_branch: staging
          commit_message: "Deploy: ${{ github.event.head_commit.message || 'manual deploy' }}"
